rules_version = '2';
// Firebase Storage → GCS bucket. Locked down until auth lands.
service firebase.storage {
  match /b/{bucket}/o {
    match /households/{householdId}/{allPaths=**} {
      allow read, write: if false;
    }
  }
}
